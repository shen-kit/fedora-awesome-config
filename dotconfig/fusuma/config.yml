swipe:
  4:
    up:
      command: amixer set Master "2%+"; awesome-client "require('beautiful').volume.update()"
    down:
      command: amixer set Master "2%-"; awesome-client "require('beautiful').volume.update()"

pinch:
  in:
    command: 'xdotool key ctrl+minus'
  out:
     command: 'xdotool key ctrl+plus'

threshold:
  swipe: 0
  pinch: 0.2

interval:
  swipe: 0.2
  pinch: 0.1
